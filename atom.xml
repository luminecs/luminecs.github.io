<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>luminecs blog</title>
  
  
  <link href="http://example.com/atom.xml" rel="self"/>
  
  <link href="http://example.com/"/>
  <updated>2021-07-02T22:15:00.223Z</updated>
  <id>http://example.com/</id>
  
  <author>
    <name>lumine</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>Hello World</title>
    <link href="http://example.com/2021/07/03/hello-world/"/>
    <id>http://example.com/2021/07/03/hello-world/</id>
    <published>2021-07-02T22:15:00.223Z</published>
    <updated>2021-07-02T22:15:00.223Z</updated>
    
    <content type="html"><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p><h2 id="quick-start"><a class="markdownIt-Anchor" href="#quick-start"></a> Quick Start</h2><h3 id="create-a-new-post"><a class="markdownIt-Anchor" href="#create-a-new-post"></a> Create a new post</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p><h3 id="run-server"><a class="markdownIt-Anchor" href="#run-server"></a> Run server</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p><h3 id="generate-static-files"><a class="markdownIt-Anchor" href="#generate-static-files"></a> Generate static files</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p><h3 id="deploy-to-remote-sites"><a class="markdownIt-Anchor" href="#deploy-to-remote-sites"></a> Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;Welcome to &lt;a href=&quot;https://hexo.io/&quot;&gt;Hexo&lt;/a&gt;! This is your very first post. Check &lt;a href=&quot;https://hexo.io/docs/&quot;&gt;documentation&lt;/a&gt; for</summary>
      
    
    
    
    
  </entry>
  
  <entry>
    <title>[Transcription] Processing Data with Java SE 8 Streams, Part1</title>
    <link href="http://example.com/2021/07/02/java-8/"/>
    <id>http://example.com/2021/07/02/java-8/</id>
    <published>2021-07-02T02:35:12.000Z</published>
    <updated>2021-07-03T02:40:41.471Z</updated>
    
    <content type="html"><![CDATA[<p>by Raoul-Gabriel Urma</p><p><a href="https://www.oracle.com/technical-resources/articles/java/ma14-java-se-8-streams.html">Original link</a></p><p>概要：Java SE 8 引入 Streams，能够把集合转成 Stream，进而更加快捷的操作集合，并且代码更简洁。并行流 <code>parallelStream</code> 充分利用多核，把任务拆分为多个子任务，再把多个子任务分配到多个 CPU 上并行执行，不按照原代码顺序执行，可能存在线程安全问题，所以并发场景应该配合 JUC 使用，比如使用 CopyOnWriteArrayList 代替 ArrayList。</p><p><strong>Use stream operations to express <span style="background-color:gainsboro;">sophisticated</span> data processing queries.</strong></p><p>What would you do without collections? Nearly every Java application <em>makes</em> and <em>processes</em> collections. They are fundamental to many programming tasks: they let you group and process data. For example, you might want to create a collection of banking transactions to represent a customer’s statement. Then, you might want to process the whole collection to find out how much money the customer spent. Despite their importance, processing collections <span style="background-color:gainsboro;">is far from perfect</span> in Java.</p><p>First, typical processing patterns on collections are similar to SQL-like operations such as “finding” (for example, find the transaction with highest value) or “grouping” (for example, group all transactions related to <span style="background-color:gainsboro;">grocery</span> shopping). Most databases let you specify such operations declaratively. For example, the following SQL query lets you find the transaction ID with the highest value: <code>SELECT id, MAX(value) from transactions</code>.</p><p>As you can see, we don’t need to implement <em>how</em> to calculate the maximum value (for example, using loops and a variable to track the highest value). We only express <em>what</em> we expect. This basic idea means that you need to worry less about how to explicitly implement such queries–it is handled for you. Why can’t we do something similar with collections? How many times do you find yourself reimplementing these operations using loops over and over again?</p><p>Second, how can we process really large collections efficiently? Ideally, to <span style="background-color:gainsboro;">speed up</span> the processing, you want to <span style="background-color:gainsboro;">leverage</span> multicore architectures. However, writing parallel code is hard and <span style="background-color:gainsboro;">error-prone</span>.</p><blockquote><p>Java SE 8 <span style="background-color:gainsboro;">to the rescue</span>! The Java API designers are updating the API with a new abstraction called <em>Stream</em> that lets you process data in a declarative way. Furthermore, <u>streams can leverage multi-core architectures without you having to write a single line of multithread code</u>. Sounds good, doesn’t it? That’s what this series of articles will explore.</p><p>Here’s a <em>mind-blowing</em> idea: these two operations can produce elements “forever”.</p></blockquote><p>Before we explore in detail what you can do with streams, let’s take a look an example so you have a sense of the new programming style with Java SE 8 streams. Let’s say we need to find all transactions of type grocery and return a list of transaction IDs in decreasing order of transaction value. In Java SE 7, we’d do that as shown in <strong>Listing 1</strong>. In Java SE 8, we’d do it as shown in <strong>Listing 2</strong>.</p><p><strong>Listing 1:</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">List&lt;Transaction&gt; groceryTransactions = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line"><span class="keyword">for</span> (Transaction t : groceryTransactions) &#123;</span><br><span class="line">    <span class="keyword">if</span> (t.getType() == Transaction.GROCERY) &#123;</span><br><span class="line">        groceryTransactions.add(t);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">Collections.sort(groceryTransactions, <span class="keyword">new</span> Comparator&lt;Transaction&gt;() &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">compare</span><span class="params">(Transaction o1, Transaction o2)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> o2.getValue().compareTo(o1.getValue());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br><span class="line">List&lt;Integer&gt; transactionIds = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line"><span class="keyword">for</span> (Transaction t : groceryTransactions) &#123;</span><br><span class="line">    transactionIds.add(t.getId());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>Listing 2:</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">List&lt;Integer&gt; transactionIds = transactions.stream()</span><br><span class="line">        .filter(t -&gt; t.getType() == Transaction.GROCERY)</span><br><span class="line">        .sorted(Comparator.comparing(Transaction::getValue).reversed())</span><br><span class="line">        .map(Transaction::getId)</span><br><span class="line">        .collect(Collectors.toList());</span><br></pre></td></tr></table></figure><p><strong>Figure 1</strong> illustrates the Java SE 8 code. <u>First, we obtain a stream from the list of transactions (the data) using the <code>stream()</code> method available on <code>List</code>. Next, several operations (<code>filter</code>, <code>sorted</code>, <code>map</code>, <code>collect</code>) are chained together to form a pipeline, which can be seen as forming a query on the data.</u></p><p><img src="/images/java8/2179048.webp" alt="Figure 1" title="Figure 1" /></p><p><strong>So how about parallelizing the code?</strong> In Java SE 8 it’s easy: <u>just replace <code>stream()</code> with <strong><code>parallel Stream()</code></strong>, as shown in <strong>Listing 3</strong>, and the Streams API will internally <span style="background-color:gainsboro;">decompose</span> your query to leverage the multiple cores on your computer</u>.</p><p><strong>Listing 3</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">List&lt;Integer&gt; transactionIds = transactions.parallelStream()</span><br><span class="line">        .filter(t -&gt; t.getType() == Transaction.GROCERY)</span><br><span class="line">        .sorted(Comparator.comparing(Transaction::getValue).reversed())</span><br><span class="line">        .map(Transaction::getId)</span><br><span class="line">        .collect(Collectors.toList());</span><br></pre></td></tr></table></figure><p>Don’t worry if this code is slightly <span style="background-color:gainsboro;">overwhelming</span>. We will explore how it works in the next sections. However, notice the use of lambda expressions (for example, <code>t -&gt; t.getType() == Transaction.GROCERY</code>) and method references (for example, <code>Transaction::getId</code>), which you should be familiar with <span style="background-color:gainsboro;">by now</span>. (To <span style="background-color:gainsboro;">brush up</span> on lambda expressions, refer to previous <em>Java Magazine</em> articles and other resources listed at the end of this article.)</p><p>For now, you can see a stream as an abstraction for expressing efficient, SQL-like operations on a collection of data. In addition, these operations can be <span style="background-color:gainsboro;">succinctly</span> parameterized with lambda expressions.</p><p>At the end of this series of articles about Java SE 8 streams, you will be able to use the Streams API to write code similar to <strong>Listing 3</strong> to express powerful queries.</p><h2 id="getting-started-with-streams"><a class="markdownIt-Anchor" href="#getting-started-with-streams"></a> Getting Started with Streams</h2><p>Let’s start with a bit of theory. <strong>What’s the definition of a stream?</strong> A short definition is “<u>a sequence of elements from a source that supports aggregate operations</u>.” Let’s break it down:</p><ul><li><p><strong>Sequence of elements:</strong> A stream provides an interface to a sequenced set of values of a specific element type. <u>However, streams don’t actually store elements; they are computed on demand</u>.</p></li><li><p><strong>Source:</strong> Streams consume from a data-providing source such as <u>collections, arrays, or I/O resources</u>.</p></li><li><p><strong>Aggregate operations:</strong> Streams support SQL-like operations and common operations from functional programming languages, such as <code>filter</code>, <code>map</code>, <code>reduce</code>, <code>find</code>, <code>match</code>, <code>sorted</code>, and so on.</p></li></ul><p>Furthermore, stream operations have <u>two fundamental <span style="background-color:gainsboro;">characteristics</span></u> that make them very different from collecion operations:</p><ul><li><p><strong>Pipelining:</strong> Many stream operations return a stream themselves. This allows operations to be chained to form a larger pipeline. This enables certain optimizations, such as <em>laziness</em> and <em>short-circuiting</em>, which we explore later.</p></li><li><p><strong>Internal iteration:</strong> In <span style="background-color:gainsboro;">contrast</span> to collections, which are iterated explicitly (external iteration), stream operations do the iteration behind the scenes for you.</p></li></ul><p>Let’s revisit our earlier code example to explain these ideas. <strong>Figure 2</strong> illustrates <strong>Listing 2</strong> in more detail.</p><p><img src="/images/java8/2179051.webp" alt="Figure 2" title="Figure 2" /></p><p>We first get a stream from the list of transactions by calling the <code>stream()</code> method. The datasource is the list of transactions and will be providing a sequence of elements to the stream. Next, we apply a series of aggregate operations on the stream: <code>filter</code> (to filter elements given a predicate), <code>sorted</code> (to sort the elements given a comparator), and <code>map</code> (to extract information). All these operations <span style="background-color:gainsboro;">except</span> <code>collect</code> return a <code>Stream</code> so they can be chained to form a pipeline, which can be viewed as query on the source.</p><p>No work is actually done until <code>collect</code> is invoked. The <code>collect</code> operation will start processing the pipeline to return a result (something that is not a <code>Stream</code>; here, a <code>List</code>). Don’t worry about <code>collect</code> for now; we will explore it in detail in a future article. At the moment, <span style="background-color:gainsboro;">you can see <code>collect</code> as an operation that takes as an argument various recipes for accumulating the elements of a stream into a summary result</span>. Here, <code>toList()</code> describes a recipe for converting a <code>Stream</code> into a <code>List</code>.</p><p>Before we explore the different methods available on a stream, it is good to pause and reflect on the conceptual difference between a stream and a collection.</p><h2 id="streams-span-stylebackground-colorgainsboroversusspan-collections"><a class="markdownIt-Anchor" href="#streams-span-stylebackground-colorgainsboroversusspan-collections"></a> Streams <span style="background-color:gainsboro;">Versus</span> Collections</h2><p>Both the existing Java <span style="background-color:gainsboro;">notion</span> of collections and the new notion of streams provide interfaces to a sequence of elements. <strong>So what’s the difference?</strong> <u>In a nutshell, collections are about data and streams are about computations</u>.</p><p>Consider a movie stored on a DVD. This is a collection (perhaps of bytes or perhaps of <span style="background-color:gainsboro;">frames</span>–we don’t care which here) because it contains the whole data structure. Now consider watching the same video when it is being streamed over the internet. It is now a stream (of bytes or frames). The streaming video player needs to have downloaded only a few frames <span style="background-color:gainsboro;">in advance of</span> where the user is watching, so you can start displaying values from the beginning of the stream before most of the values in the stream have even been computed (consider streaming a live football game).</p><p><u><span style="background-color:gainsboro;">In the coarsest terms</span>, the difference between collections and streams has to do with <em>when</em> things are computed. A collection is an in-memory data structure, which holds all the values that the data structure currently has–every element in the collection has to be computed before it can be added to the collection. In contrast, a stream is a conceptually fixed data structure in which elements are computed on demand.</u></p><p>Using the <code>Collection</code> interface requires iteration to be done by the user (for example, using the enhance <code>for</code> loop called <code>foreach</code>); this is called <strong>external iteration</strong>.</p><p>In contrast, the Streams library uses <strong>internal iteration</strong>–it does the iteration for you and takes care of storing the resulting stream value somewhere; you merely provide a function saying what’s to be done. The code in <strong>Listing 4</strong> (external iteration with a collection) and <strong>Listing 5</strong> (internal iteration with a stream) illustrates this difference.</p><p><strong>Listing 4:</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">List&lt;String&gt; transactionIds = <span class="keyword">new</span> ArrayList&lt;&gt;(); </span><br><span class="line"><span class="keyword">for</span>(Transaction t: transactions)&#123;</span><br><span class="line">    transactionIds.add(t.getId()); </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>Listing 5:</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">List&lt;Integer&gt; transactionIds = transactions.stream()</span><br><span class="line">                .map(Transaction::getId)</span><br><span class="line">                .collect(toList());</span><br></pre></td></tr></table></figure><p>In <strong>Listing 4</strong>, we explicitly iterate the list of transactions sequentially to extract each transaction ID and add it to an <span style="background-color:gainsboro;">accumulator</span>. In contrast, when using a stream, there’s no explicit iteration. The code in <strong>Listing 5</strong> build a query, where the <code>map</code> operation is parameterized to extract the transaction IDs and the <code>collect</code> operation converts the resulting <code>Stream</code> into a <code>List</code>.</p><p>You should now have a good idea of what a stream is and what you can do with it. Let’s now look at the different operations supported by streams so you can express your own data processing queries.</p><p>总结：Collection 是一个有结构的水桶（data structure），里面存储水（data）。Stream 是一个管道流（pipeline），可以从水桶中提取水（extract or query data），并进行相应的处理（map,filter,sorted…），生产（collect…）出你想要的各种饮品（茅台、五粮液、可乐、咖啡…）。如果你觉得生产效率有点慢，甚至可以启用并行流（parallel stream）。</p><h2 id="stream-operations-exploiting-streams-to-process-data"><a class="markdownIt-Anchor" href="#stream-operations-exploiting-streams-to-process-data"></a> Stream Operations: Exploiting Streams to Process Data</h2><p>The <code>Stream</code> interface in <code>java.util.stream.Stream</code> defines many operations, which can be grouped in two categories. In the example illustrated in <strong>Figure 1</strong>, you can see the following operations:</p><ul><li><code>filter</code>, <code>sorted</code>, and <code>map</code>, which can be connected together to form a pipeline</li><li><code>collect</code>, which closed the pipeline and returned a result</li></ul><p>Stream operations that can be connected are called <em><strong>intermediate operations</strong></em>. <u>They can be connected together because their return type is a <code>Stream</code></u>. Operations that close a stream pipeline are called <em><strong>terminal operations</strong></em>. They produce a result from a pipeline such as a <code>List</code>, an <code>Integer</code>, or even <code>void</code> (any non-Stream type).</p><p>You might be wondering why the distinction is important. Well, <u>intermediate operations do not perform any processing until a terminal operation is invoked on the stream pipeline; they are “lazy”</u>. This is because intermediate operations can usually be “merged” and processed into a single pass by the terminal operation.</p><p><strong>Listing 6:</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">List&lt;Integer&gt; numbers = Arrays.asList(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>, <span class="number">7</span>, <span class="number">8</span>);</span><br><span class="line">List&lt;Integer&gt; twoEvenSquares = numbers.stream()</span><br><span class="line">        .filter(n -&gt; &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;filtering &quot;</span> + n);</span><br><span class="line">            <span class="keyword">return</span> n % <span class="number">2</span> == <span class="number">0</span>;</span><br><span class="line">        &#125;)</span><br><span class="line">        .map(n -&gt; &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;mapping &quot;</span> + n);</span><br><span class="line">            <span class="keyword">return</span> n * n;</span><br><span class="line">        &#125;)</span><br><span class="line">        .limit(<span class="number">2</span>)</span><br><span class="line">        .collect(Collectors.toList());</span><br></pre></td></tr></table></figure><blockquote><p>注意：此处的 limit 数量，是对 filter 之后的数据而言的</p></blockquote><p>For example, consider the code in <strong>Listing 6</strong>, which computes two even square numbers from a given list of numbers. You might be surprised that it prints the following:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">filtering 1</span><br><span class="line">filtering 2</span><br><span class="line">mapping 2</span><br><span class="line">filtering 3</span><br><span class="line">filtering 4</span><br><span class="line">mapping 4</span><br></pre></td></tr></table></figure><p>This is because <code>limit(2)</code> uses <strong>short-circuiting</strong>; we need to process only part of the stream, not all of it, to return a result. This is similar to evaluating a large Boolean expression chained with the <code>and</code> operator: <span style="background-color:gainsboro;">as soon as</span> one expression returns <code>false</code>, we can <span style="background-color:gainsboro;">deduce</span> that the whole expression is <code>false</code> without evaluating all of it. Here, the operation <code>limit</code> returns a stream of size 2.</p><p>In addition, the operations <code>filter</code> and <code>map</code> have been merged in the same pass.</p><p>To summarize what we’ve learned so far, working with streams, in general, involves three things:</p><ul><li><u>A datasource (such as a collection) on which to perform a query</u></li><li><u>A chain of intermediate operations, which form a stream pipeline</u></li><li><u>One terminal operation, which executes the stream pipeline and produces a result</u></li></ul><blockquote><p>The Streams API will internally decompose your query to leverage the multiple cores on your computer.</p></blockquote><p>Let’s now take a tour of some of the operations available on streams. Refer to the <code>java.util.stream.Stream</code> interface for the complete list, as well as to resources at the end of this article for more examples.</p><h3 id="filtering"><a class="markdownIt-Anchor" href="#filtering"></a> Filtering</h3><p>These are several operations that can be used to filter elements from a stream:</p><ul><li><code>filter(Predicate)</code>: Takes a predicate (<code>java.util.function.Predicate</code>) as an argument and returns a stream including all elements that match the given predicate.</li><li><code>distinct</code>: Returns a stream with unique element (according to the implements of <code>equals</code> for a stream element)</li><li><code>limit(n)</code>: Returns a stream that is no longer than the given size <code>n</code></li><li><code>skip(n)</code>: Returns a stream with the first n number of elements discarded</li></ul><h3 id="finding-and-matching"><a class="markdownIt-Anchor" href="#finding-and-matching"></a> Finding and matching</h3><p>A common data processing pattern is determining whether some elements match a given property. You can use the <code>anyMatch</code>, <code>allMatch</code>, and <code>noneMatch</code> operations to help you do this. They all take a predicate as an argument and return a <code>boolean</code> as the result (they are, therefore, terminal operations). For example, you can use <code>allMatch</code> to check that all elements in a stream of transactions have a value higher than 100, as shown in <strong>Listing 7</strong>.</p><p><strong>Listing 7:</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">boolean</span> expensive = transactions.stream()</span><br><span class="line">                .allMatch(t -&gt; t.getValue() &gt; <span class="number">100</span>);</span><br></pre></td></tr></table></figure><p><strong>Listing 8:</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Optional&lt;Transaction&gt; = transactions.stream()</span><br><span class="line">        .filter(t -&gt; t.getType() == Transaction.GROCERY)</span><br><span class="line">        .findAny();</span><br></pre></td></tr></table></figure><p><u>The <code>Optional&lt;T&gt;</code> class (java.util.Optional) is a container class to represent the existence or absence of value</u>. In <strong>Listing 8</strong>, it is possible that <code>findAny</code> doesn’t find any transaction of type <code>grocery</code>. The <code>Optional</code> class contains several methods to test the existence of an element. For example, if a transaction is present, we can choose to apply an operation on the optional object by using the <code>ifPresent</code> method, as shown in <strong>Listing 9</strong> (where we just print the transaction).</p><p><strong>Listing 9:</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">Optional&lt;Transaction&gt; = transactions.stream()</span><br><span class="line">        .filter(t -&gt; t.getType() == Transaction.GROCERY)</span><br><span class="line">        .findAny()</span><br><span class="line">        .ifPresent(System.out::println);</span><br></pre></td></tr></table></figure><h3 id="mapping"><a class="markdownIt-Anchor" href="#mapping"></a> Mapping</h3><p>Streams support the method <code>map</code>, which <u>takes a function (<code>java.util.function.Function</code>) as an argument to project the elements of stream into another form. The function is applied to each element, “mapping” it into a new element</u>.</p><p>For example, you might want to use it to extract information from each element of a stream. In the example in <strong>Listing 10</strong>, we return a list of the length of each word from a list.</p><p><strong>Listing 10:</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">List&lt;String&gt; words = Arrays.asList(<span class="string">&quot;Oracle&quot;</span>, <span class="string">&quot;Java&quot;</span>, <span class="string">&quot;Magazine&quot;</span>);</span><br><span class="line">List&lt;Integer&gt; wordLengths = words.stream()</span><br><span class="line">        .map(String::length)</span><br><span class="line">        .collect(Collectors.toList());</span><br></pre></td></tr></table></figure><h3 id="reducing"><a class="markdownIt-Anchor" href="#reducing"></a> Reducing</h3><p>So far, the terminal operations we’ve seen return a <code>boolean</code> (<code>allMatch</code> and so on), <code>void (forEach)</code>, or an <code>Optional</code> object (<code>findAny</code> and so on). We have also been using <code>collect</code> to combine all elements in a <code>Stream</code> into a <code>List</code>.</p><p>However, you can also combine all elements in a stream to <span style="background-color:gainsboro;">formulate more-complicated</span> process queries, such as “what is the transaction with the highest ID?” or “calculate the sum of all transactions’ values.” <u>This is possible using the <code>reduce</code> operation on streams, which repeatedly applies an operation (for example, adding two numbers) on each element until a result is produced. It’s often called a <em><strong>fold operation</strong></em> in functional programming because you can view this operation as “folding” repeatedly a long piece of paper (your stream) until it forms one little square, which is the result of the fold operation.</u></p><p>It helps to first look at how we could calculate the sum of a list using a <code>for</code> loop:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">int</span> sum = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">int</span> x : numbers) &#123;</span><br><span class="line">    sum += x; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Each element of the list of numbers is combined iteratively using the addition operator to produce a result. <u>We essentially “reduced” the list of numbers into one number.</u> These are <u>two parameters</u> in this code: the initial value of the <code>sum</code> variable, in this case <code>0</code>, and the operation for combining all the elements of the list, in this case <code>+</code>.</p><p>Using the <code>reduce</code> method on streams, we can sum all the elements of a stream as shown in <strong>Listing 11</strong>.</p><p><strong>Listing 11:</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">int</span> sum = numbers.stream().reduce(<span class="number">0</span>, (a, b) -&gt; a + b);</span><br></pre></td></tr></table></figure><p>The <code>reduce</code> method takes <u>two arguments</u>:</p><ul><li>An initial value, here <code>0</code></li><li>A <code>BinaryOperator&lt;T&gt;</code> to combine two elements and produce a new value</li></ul><p><u>The <code>reduce</code> method essentially abstracts the pattern of repeated application</u>. Other queries such as “calculate the product” or “calculate the maximum” (see <strong>Listing 12</strong>) become special use cases of the <code>reduce</code> method.</p><p><strong>Listing 12:</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">int</span> product = numbers.stream().reduce(<span class="number">1</span>, (a, b) -&gt; a * b);</span><br><span class="line"><span class="keyword">int</span> product = numbers.stream().reduce(<span class="number">1</span>, Integer::max);</span><br></pre></td></tr></table></figure>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;by Raoul-Gabriel Urma&lt;/p&gt;
&lt;p&gt;&lt;a href=&quot;https://www.oracle.com/technical-resources/articles/java/ma14-java-se-8-streams.html&quot;&gt;Original link</summary>
      
    
    
    
    <category term="Java 8" scheme="http://example.com/categories/Java-8/"/>
    
    
    <category term="Java" scheme="http://example.com/tags/Java/"/>
    
  </entry>
  
</feed>
